import 'package:flutter/material.dart';

class mobailScreenLayout extends StatelessWidget {
  const mobailScreenLayout({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Center(
        child: Text("It is mobail layout"),
      ),
    );
  }
}
